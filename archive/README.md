# Archive

This directory holds preserved historical material that should not clutter the main quickstart, but also should not be deleted casually.

## Contents

- `legacy/LISA-clone/` — a preserved nested duplicate of the original repo payload. It appeared to match the root payload at cleanup time, except for root-only `.gitignore` and `.memory_meta.json`.

Keeping this here follows the cleanup rule: **if unsure, move it aside; do not erase it.**
